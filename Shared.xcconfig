// Shared build settings for app + widget extension so version
// numbers can never diverge (ITMS-90473). Both targets inherit
// MARKETING_VERSION and CURRENT_PROJECT_VERSION from this file.

MARKETING_VERSION = 1.0
CURRENT_PROJECT_VERSION = 1
