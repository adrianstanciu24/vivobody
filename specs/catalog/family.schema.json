{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vivobody.local/schema/catalog-family.schema.json",
  "title": "Vivobody catalog movement family",
  "description": "Authoring contract for one movement family and its explicitly reviewed exercises.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "id",
    "name",
    "definition",
    "evidenceRefs",
    "fixed",
    "groupPolicy",
    "allowed",
    "movementSignature",
    "musclePolicy",
    "variantAxes",
    "exerciseRules",
    "exercises"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "schemaVersion": {
      "const": 1
    },
    "id": {
      "$ref": "#/$defs/stableID"
    },
    "name": {
      "$ref": "#/$defs/nonEmptyString"
    },
    "definition": {
      "$ref": "#/$defs/nonEmptyString"
    },
    "evidenceRefs": {
      "$ref": "#/$defs/evidenceRefs"
    },
    "fixed": {
      "$ref": "#/$defs/fixedClassification"
    },
    "groupPolicy": {
      "$ref": "#/$defs/groupPolicy"
    },
    "allowed": {
      "$ref": "#/$defs/allowedClassification"
    },
    "movementSignature": {
      "$ref": "#/$defs/movementSignature"
    },
    "musclePolicy": {
      "$ref": "#/$defs/musclePolicy"
    },
    "variantAxes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/variantAxis"
      }
    },
    "exerciseRules": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/exerciseRule"
      }
    },
    "recommended": {
      "$ref": "#/$defs/recommendedDefaults"
    },
    "exercises": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/exercise"
      }
    }
  },
  "$defs": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "stableID": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "symbolID": {
      "type": "string",
      "pattern": "^[a-z][A-Za-z0-9]*$"
    },
    "actionID": {
      "type": "string",
      "pattern": "^[a-z][A-Za-z0-9]*\\.[a-z][A-Za-z0-9]*$"
    },
    "actionRequirement": {
      "oneOf": [
        {
          "$ref": "#/$defs/actionID"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "action",
            "condition"
          ],
          "properties": {
            "action": {
              "$ref": "#/$defs/actionID"
            },
            "condition": {
              "$ref": "#/$defs/symbolID"
            }
          }
        }
      ]
    },
    "movementPhase": {
      "description": "One ordered phase of a multi-phase repetition. Prime actions are produced, resisted actions are opposed without occurring, and yielding actions occur while muscles capable of the opposite action control them eccentrically.",
      "type": "object",
      "additionalProperties": false,
      "anyOf": [
        {
          "properties": {
            "primeActions": {
              "minItems": 1
            }
          }
        },
        {
          "required": [
            "resistedActions"
          ]
        },
        {
          "required": [
            "yieldingActions"
          ]
        }
      ],
      "required": [
        "id",
        "name",
        "primeActions"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/stableID"
        },
        "name": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "primeActions": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        },
        "resistedActions": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        },
        "yieldingActions": {
          "description": "Externally driven joint motion that occurs while assigned movers capable of the centrally mapped opposite action control the load eccentrically.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        }
      }
    },
    "role": {
      "type": "string",
      "enum": [
        "primary",
        "secondary",
        "stabilizer"
      ]
    },
    "muscleGroup": {
      "type": "string",
      "enum": [
        "chest",
        "back",
        "shoulders",
        "arms",
        "core",
        "legs"
      ]
    },
    "evidenceRefs": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/stableID"
      }
    },
    "fixedClassification": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "mechanic",
        "trainingRole",
        "pattern",
        "direction",
        "planes"
      ],
      "properties": {
        "mechanic": {
          "type": "string",
          "enum": [
            "compound",
            "isolation"
          ]
        },
        "trainingRole": {
          "description": "Authored programming placement used for cross-mechanic discovery and mechanic-separated analysis; not an anatomical joint action.",
          "type": "string",
          "enum": [
            "push",
            "pull",
            "legs",
            "core",
            "other"
          ]
        },
        "pattern": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "push",
            "pull",
            "squat",
            "hinge",
            "lunge",
            "carry",
            "core",
            "hang",
            null
          ]
        },
        "direction": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "horizontal",
            "vertical",
            "diagonal",
            null
          ]
        },
        "planes": {
          "description": "Exact family-level cardinal-plane surface reviewed through planeBasisActions. Every exercise uses every listed plane at the basis region because family prime and resisted actions are roster-wide invariants; actions at other joint regions retain their own planes.",
          "type": "array",
          "minItems": 1,
          "maxItems": 3,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "sagittal",
              "frontal",
              "transverse"
            ]
          }
        }
      }
    },
    "groupPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "default",
        "allowed"
      ],
      "properties": {
        "default": {
          "$ref": "#/$defs/muscleGroup"
        },
        "allowed": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/muscleGroup"
          }
        }
      }
    },
    "allowedClassification": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "equipment",
        "modalities",
        "trackingModes",
        "loadModes",
        "lateralities"
      ],
      "properties": {
        "equipment": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "barbell",
              "dumbbell",
              "cable",
              "machine",
              "bodyweight",
              "kettlebell",
              "band",
              "gripTrainer",
              "trapBar",
              "other"
            ]
          }
        },
        "modalities": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "dynamicStrength",
              "isometricStrength",
              "power"
            ]
          }
        },
        "trackingModes": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "reps",
              "duration"
            ]
          }
        },
        "loadModes": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "external",
              "bodyweightAdded",
              "assistanceSubtracted",
              "nonComparable"
            ]
          }
        },
        "lateralities": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": [
              "bilateral",
              "unilateral"
            ]
          }
        }
      }
    },
    "movementSignature": {
      "description": "Prime actions describe joint motion produced by the repetition. Resisted actions describe an external joint-action tendency that assigned movers oppose without producing that tendency.",
      "type": "object",
      "additionalProperties": false,
      "anyOf": [
        {
          "properties": {
            "primeActions": {
              "minItems": 1
            }
          }
        },
        {
          "required": [
            "resistedActions"
          ]
        },
        {
          "required": [
            "movementPhases"
          ]
        }
      ],
      "required": [
        "planeBasisActions",
        "primeActions",
        "stabilityDemands"
      ],
      "properties": {
        "planeBasisActions": {
          "description": "Family-level prime or resisted actions whose plane union exactly defines fixed.planes.",
          "type": "array",
          "minItems": 1,
          "maxItems": 3,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionID"
          }
        },
        "primeActions": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        },
        "resistedActions": {
          "description": "External action tendencies opposed throughout the family; the validator requires a primary or secondary muscle capable of the centrally mapped opposite action.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        },
        "movementPhases": {
          "description": "Ordered phases for a Power repetition or controlled-yield Dynamic Strength repetition whose defining joint actions cannot be represented truthfully as one concentric action set.",
          "type": "array",
          "minItems": 2,
          "items": {
            "$ref": "#/$defs/movementPhase"
          }
        },
        "forbiddenPrimeActions": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionID"
          }
        },
        "stabilityDemands": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/symbolID"
          }
        }
      }
    },
    "muscleRequirement": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "anyOf",
        "minimumRole"
      ],
      "properties": {
        "anyOf": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/symbolID"
          }
        },
        "minimumRole": {
          "$ref": "#/$defs/role"
        }
      }
    },
    "musclePolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "requirements",
        "allowedByRole"
      ],
      "properties": {
        "requirements": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/muscleRequirement"
          }
        },
        "allowedByRole": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "primary",
            "secondary",
            "stabilizer"
          ],
          "properties": {
            "primary": {
              "$ref": "#/$defs/muscleIDList"
            },
            "secondary": {
              "$ref": "#/$defs/muscleIDList"
            },
            "stabilizer": {
              "$ref": "#/$defs/muscleIDList"
            }
          }
        }
      }
    },
    "muscleIDList": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/symbolID"
      }
    },
    "variantAxis": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "valueType",
        "required",
        "description"
      ],
      "allOf": [
        {
          "if": {
            "required": [
              "fixedValue"
            ]
          },
          "then": {
            "properties": {
              "valueType": {
                "const": "boolean"
              },
              "required": {
                "const": true
              }
            }
          }
        }
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/symbolID"
        },
        "valueType": {
          "type": "string",
          "enum": [
            "enum",
            "number",
            "boolean",
            "string"
          ]
        },
        "required": {
          "type": "boolean"
        },
        "description": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "allowedValues": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string"
          }
        },
        "minimum": {
          "type": "number"
        },
        "maximum": {
          "type": "number"
        },
        "fixedValue": {
          "type": "boolean"
        }
      }
    },
    "ruleFieldPath": {
      "type": "string",
      "pattern": "^[a-z][A-Za-z0-9]*(?:\\.[a-z][A-Za-z0-9]*)?$"
    },
    "rulePredicate": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "field",
        "operator",
        "value"
      ],
      "properties": {
        "field": {
          "$ref": "#/$defs/ruleFieldPath"
        },
        "operator": {
          "type": "string",
          "enum": [
            "equals",
            "notEquals"
          ]
        },
        "value": {
          "$ref": "#/$defs/variantValue"
        }
      }
    },
    "ruleAssertion": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "field"
      ],
      "oneOf": [
        {
          "required": [
            "value"
          ],
          "not": {
            "required": [
              "allowedValues"
            ]
          }
        },
        {
          "required": [
            "allowedValues"
          ],
          "not": {
            "required": [
              "value"
            ]
          }
        }
      ],
      "properties": {
        "field": {
          "$ref": "#/$defs/ruleFieldPath"
        },
        "value": {
          "$ref": "#/$defs/variantValue"
        },
        "allowedValues": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/variantValue"
          }
        }
      }
    },
    "exerciseRule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "description",
        "when",
        "then",
        "requirePresent",
        "requireAbsent"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/stableID"
        },
        "description": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "when": {
          "$ref": "#/$defs/rulePredicate"
        },
        "then": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ruleAssertion"
          }
        },
        "requirePresent": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/ruleFieldPath"
          }
        },
        "requireAbsent": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/ruleFieldPath"
          }
        },
        "requireInvolvement": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/muscleAssignment"
          }
        },
        "requireMuscleRequirements": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/muscleRequirement"
          }
        },
        "requireAdditionalPrimeActions": {
          "description": "The exact exercise-level prime-action exception set required when this rule matches; an empty array forbids exercise-level broadening for the matched fixture.",
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        },
        "requireAdditionalStabilityDemands": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/symbolID"
          }
        }
      }
    },
    "recommendedDefaults": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "defaultReps": {
          "$ref": "#/$defs/integerRange"
        },
        "defaultDuration": {
          "$ref": "#/$defs/numberRange"
        }
      }
    },
    "integerRange": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "minimum",
        "maximum"
      ],
      "properties": {
        "minimum": {
          "type": "integer",
          "minimum": 1
        },
        "maximum": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "numberRange": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "minimum",
        "maximum"
      ],
      "properties": {
        "minimum": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "maximum": {
          "type": "number",
          "exclusiveMinimum": 0
        }
      }
    },
    "muscleAssignment": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "muscle",
        "role"
      ],
      "properties": {
        "muscle": {
          "$ref": "#/$defs/symbolID"
        },
        "role": {
          "$ref": "#/$defs/role"
        }
      }
    },
    "variantValue": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "number"
        },
        {
          "type": "boolean"
        }
      ]
    },
    "exercise": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "catalogID",
        "name",
        "aliases",
        "equipment",
        "laterality",
        "modality",
        "trackingMode",
        "loadMode",
        "bodyweightFraction",
        "defaultWeight",
        "reps",
        "involvement",
        "variant",
        "additionalPrimeActions",
        "additionalStabilityDemands",
        "evidenceRefs",
        "execution"
      ],
      "properties": {
        "catalogID": {
          "$ref": "#/$defs/stableID"
        },
        "name": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "aliases": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/nonEmptyString"
          }
        },
        "groupOverride": {
          "$ref": "#/$defs/muscleGroup"
        },
        "equipment": {
          "type": "string"
        },
        "laterality": {
          "type": "string"
        },
        "modality": {
          "type": "string"
        },
        "trackingMode": {
          "type": "string"
        },
        "loadMode": {
          "type": "string"
        },
        "bodyweightFraction": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "defaultWeight": {
          "type": "number",
          "minimum": 0
        },
        "defaultWeightKg": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "reps": {
          "type": "integer",
          "minimum": 1
        },
        "defaultDuration": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "searchPriority": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "involvement": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/muscleAssignment"
          }
        },
        "variant": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/variantValue"
          }
        },
        "additionalPrimeActions": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/actionRequirement"
          }
        },
        "additionalStabilityDemands": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/symbolID"
          }
        },
        "evidenceRefs": {
          "$ref": "#/$defs/evidenceRefs"
        },
        "execution": {
          "$ref": "#/$defs/executionInstructions"
        }
      }
    },
    "executionInstructions": {
      "description": "Structured execution contract separating sequential phases from concurrent constraints. The compiler enforces two conditional fields against the exercise's declared trackingMode, laterality, and family pattern: returnPhase is required for rep-tracked exercises and forbidden for duration-tracked ones; sideOrDirection is required for unilateral exercises and carry-pattern families and forbidden otherwise.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "startingPosition",
        "movement",
        "endpoint",
        "controlledJoints",
        "supportAndPosture",
        "disqualifyingCompensations"
      ],
      "properties": {
        "startingPosition": {
          "description": "Setup, stance, grip, and load placement.",
          "$ref": "#/$defs/nonEmptyString"
        },
        "movement": {
          "description": "The intended movement.",
          "$ref": "#/$defs/nonEmptyString"
        },
        "endpoint": {
          "description": "Where the movement ends; for holds, the held position.",
          "$ref": "#/$defs/nonEmptyString"
        },
        "returnPhase": {
          "description": "How to return to the start; required exactly when trackingMode is reps.",
          "$ref": "#/$defs/nonEmptyString"
        },
        "controlledJoints": {
          "description": "Joints that must remain controlled or still.",
          "$ref": "#/$defs/nonEmptyString"
        },
        "supportAndPosture": {
          "description": "Support surfaces, bracing, and posture.",
          "$ref": "#/$defs/nonEmptyString"
        },
        "disqualifyingCompensations": {
          "description": "Each entry names a compensation and the exercise or pattern it turns the movement into.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/nonEmptyString"
          }
        },
        "sideOrDirection": {
          "description": "Side-switching or travel-direction requirement; required for unilateral exercises and carry-pattern families, forbidden otherwise.",
          "$ref": "#/$defs/nonEmptyString"
        }
      }
    }
  }
}
