# Kettlebell Press-Ups with Bands

Status: blocked pending setup confirmation.

Requested fixture: `Kettlebell Press-Ups w/ Bands`.

The available Boxing Science exercise-library text establishes a strict press-up
performed with both hands on stable kettlebell handles, neutral palms, a rigid
trunk, hip-width feet, and tucked elbows. Despite the exercise title, the text
does not say where the resistance band is placed or anchored, how many bands are
used, or whether the kettlebells anchor the band.

That missing topology changes both execution and safety. The record must not be
activated until the owner confirms the intended setup or supplies an image or
demonstration. The proposed interpretation is one band looped across the upper
back with each end secured beneath a stable kettlebell handle; both kettlebells
serve as neutral-grip hand supports. Once confirmed, this belongs in the
`horizontal-press` family as a bilateral dynamic-strength, reps-tracked,
non-comparable band exercise. Integration must add `band` to the family
equipment set, add a support-equipment axis for the paired kettlebell handles,
and replace the current suspension-only `nonComparable` rule with distinct band
and suspension branch rules. The band is the primary resistance equipment and
the kettlebells are supports, so the current single-equipment filter would list
it under Band rather than Kettlebell. The exact textual evidence is the
[Boxing Science exercise-library mirror](https://www.scribd.com/document/965522663/EXERCISE-LIBRARY-Boxing-Science-Strength-and-Conditioning-Program-Vwtlck);
activation still requires the missing band topology.

Do not activate a generic alias until the band topology is resolved.
